[pytest]
base_url = https://www.saucedemo.com/
pythonpath = .

addopts =
    --browser=chromium
    --screenshot=only-on-failure
    --tracing=retain-on-failure
    --html=report.html
    --self-contained-html